# NC - Configuração (copie para .env e ajuste os valores)

# Porta do servidor
PORT=3000

# --- Opção A: Relay no Hostinger (mesmo SMTP do recuperar senha) ---
# O Railway chama um script PHP no seu site; o PHP envia o email no Hostinger.
# Assim o SMTP é usado no próprio servidor e o código chega. Veja EMAIL-RELAY-HOSTINGER.md
# NC_EMAIL_RELAY_URL=https://claudeservices.com/nc/claude/frontend/enviar_codigo_nc.php
# NC_EMAIL_RELAY_SECRET=uma_senha_forte_aleatoria

# --- Opção B: SMTP direto (pode falhar no Railway se o Hostinger bloquear) ---
# NC_SMTP_HOST=smtp.hostinger.com
# NC_SMTP_PORT=587
# NC_SMTP_USER=nc@claudeservices.com
# NC_SMTP_PASS=senha_do_email_nc
# NC_EMAIL_FROM=Claude Services <nc@claudeservices.com>

# --- Opção C: Resend (funciona no Railway) ---
# NC_RESEND_API_KEY=re_xxxx
# NC_EMAIL_FROM=NC <onboarding@resend.dev>

# Push Notifications (opcional - gerado automaticamente se não configurado)
# NC_VAPID_PUBLIC=sua-chave-publica
# NC_VAPID_PRIVATE=sua-chave-privada

# NC - Configuração (copie para .env e ajuste os valores)

# Porta do servidor
PORT=3000

# SMTP (opcional - para envio de emails)
# Mesma config do recuperar_senha (claudeservices.com): smtp.hostinger.com, porta 587, nc@claudeservices.com
# NC_SMTP_HOST=smtp.hostinger.com
# NC_SMTP_PORT=587
# NC_SMTP_USER=nc@claudeservices.com
# NC_SMTP_PASS=mesma_senha_do_email_config_outlook_php
# NC_EMAIL_FROM=Claude Services <nc@claudeservices.com>

# Push Notifications (opcional - gerado automaticamente se não configurado)
# NC_VAPID_PUBLIC=sua-chave-publica
# NC_VAPID_PRIVATE=sua-chave-privada
